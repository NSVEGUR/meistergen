{
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "start:server": "turbo dev --filter server",
    "start:client": "turbo dev --filter client",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.7.1",
    "prettier-plugin-svelte": "^2.7.0",
    "turbo": "^1.8.3"
  },
  "packageManager": "pnpm@7.15.0"
}
